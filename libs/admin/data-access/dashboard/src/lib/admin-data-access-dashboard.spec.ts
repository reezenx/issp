import { adminDataAccessDashboard } from './admin-data-access-dashboard';

describe('adminDataAccessDashboard', () => {
  it('should work', () => {
    expect(adminDataAccessDashboard()).toEqual('admin-data-access-dashboard');
  });
});
