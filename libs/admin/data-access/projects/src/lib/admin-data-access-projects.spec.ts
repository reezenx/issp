import { adminDataAccessProjects } from './admin-data-access-projects';

describe('adminDataAccessProjects', () => {
  it('should work', () => {
    expect(adminDataAccessProjects()).toEqual('admin-data-access-projects');
  });
});
