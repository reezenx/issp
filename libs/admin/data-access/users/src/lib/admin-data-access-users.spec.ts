import { adminDataAccessUsers } from './admin-data-access-users';

describe('adminDataAccessUsers', () => {
  it('should work', () => {
    expect(adminDataAccessUsers()).toEqual('admin-data-access-users');
  });
});
