import { clientDataAccessAccount } from './client-data-access-account';

describe('clientDataAccessAccount', () => {
  it('should work', () => {
    expect(clientDataAccessAccount()).toEqual('client-data-access-account');
  });
});
