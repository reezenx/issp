import { sharedDataAccessProfile } from './shared-data-access-profile';

describe('sharedDataAccessProfile', () => {
  it('should work', () => {
    expect(sharedDataAccessProfile()).toEqual('shared-data-access-profile');
  });
});
