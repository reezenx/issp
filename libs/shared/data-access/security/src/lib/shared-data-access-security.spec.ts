import { sharedDataAccessSecurity } from './shared-data-access-security';

describe('sharedDataAccessSecurity', () => {
  it('should work', () => {
    expect(sharedDataAccessSecurity()).toEqual('shared-data-access-security');
  });
});
